mixin videoBlock(video = {})
    .videoBlock
        a(href=routes.videoDetail(video.id))
            video.videoBlock__thumbnail(src=video.videoSrc, controls=true)
            h4.videoBlock__title=video.title
            h6.videoBlock__title=video.views